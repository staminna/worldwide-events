*.db
*.db-journal
*.db-wal
*.db-shm
.env
# The compiled binary, anchored to this directory. Without the leading slash
# this also matched the cmd/eventscraper/ source dir, so main.go was never
# tracked.
/eventscraper
